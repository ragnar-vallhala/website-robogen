// next.config.js
module.exports = {
    experimental: {
      serverActions: true,
      typedRoutes: false // Disables the experimental feature causing warning
    }
  };

//You can safely ignore the experimental warning, but if you want to suppress it:

